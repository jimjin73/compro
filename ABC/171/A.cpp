#include <bits/stdc++.h>

using namespace std;
typedef long long ll;
typedef pair<ll,ll> P;

int main(){
    char c;
    cin >> c;
    if(c >= 'a' && c <= 'z'){
        cout << 'a' << endl;
    }else{
        cout << 'A' << endl;
    }

    return 0;
}